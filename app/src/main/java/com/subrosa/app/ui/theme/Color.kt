package com.subrosa.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── "Confidential Dossier" palette ──────────────────────────────────────────
// Warm archival paper, oxblood ink, a single thread of brass. Sub rosa: under the rose.

// Paper / surfaces
val Paper = Color(0xFFF1E9DA)       // aged bone paper (app background)
val PaperHigh = Color(0xFFFBF6EC)   // raised leaf (cards, plates)
val PaperEdge = Color(0xFFE6D9C2)   // inset / pressed paper
val Hairline = Color(0xFFCBBCA1)    // taupe ruling

// Ink
val Ink = Color(0xFF221B16)         // near-black warm ink
val InkSoft = Color(0xFF6A5E4F)     // sepia secondary ink
val InkFaint = Color(0xFF9C8E79)    // tertiary / metadata

// Accents
val Oxblood = Color(0xFF6E1F2A)     // primary — deep bordeaux rose
val OxbloodDeep = Color(0xFF52141D) // pressed primary
val Rose = Color(0xFF9B4A56)        // lighter rose
val Brass = Color(0xFF94762F)       // restrained gold/brass secondary
val BrassSoft = Color(0xFFC2A45F)

// Status (kept on-theme: laurel / oxblood / brass-amber)
val CoveredGreen = Color(0xFF4C6346)
val MissingRed = Color(0xFF8C2A2A)
val AmbiguousAmber = Color(0xFFA9772B)
